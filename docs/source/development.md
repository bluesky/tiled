# Development

Note: Directories are created in the current directory for scratch space. If
using uvicorn's ``--reload`` option, be sure to set ``--reload-dir=tiled``, thus
excluding the current working directory from being watched, to avoid reloading
everytime a scratch file is updated.
